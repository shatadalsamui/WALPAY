  {
  "name": "bank-webhook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx esbuild ./src/index.ts --bundle --platform=node --outfile=dist/index.js && cp -r ../../node_modules/.prisma/client dist/",
    "start": "dotenv -e ../../packages/db/.env -- sh -c 'PRISMA_QUERY_ENGINE_LIBRARY=./dist/client/libquery_engine-debian-openssl-3.0.x.so.node node dist/index.js'",
    "dev": "npm run build && npm run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@repo/db": "*",
    "@types/express": "^4.17.21",
    "dotenv": "^16.4.5",
    "dotenv-cli": "^4.0.0",
    "esbuild": "^0.20.2",
    "express": "^4.19.1"
  }
}
